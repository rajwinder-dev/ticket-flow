import { Alert } from "@/components/ui/alert";
import EmailProviderTable from "../email/EmailProviderTable";

const GeneralTab = () => {
  return (
    <div className="flex flex-col gap-4">
      <EmailProviderTable />
      <Alert className="flex flex-col gap-2 p-4">
        <div className="text-foreground font-semibold">
          Use this link to receive webhook events:
        </div>

        {/* The dynamic URL template */}
        <code className="bg-muted text-secondary-foreground w-fit rounded px-2 py-1 font-mono text-sm break-all">
          {`${window.location.origin}/webhooks/:Type`}
        </code>

        {/* The example helper text */}
        <div className="text-muted-foreground mt-1 text-xs">
          Example:
          <span className="bg-muted/50 rounded px-1.5 py-0.5 font-mono">{`${window.location.origin}/webhooks/resend`}</span>
        </div>
      </Alert>
    </div>
  );
};

export default GeneralTab;
