info:
  name: create queue (6)
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/v1/queue"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
