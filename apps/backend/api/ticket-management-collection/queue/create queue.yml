info:
  name: create queue
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/queue"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
