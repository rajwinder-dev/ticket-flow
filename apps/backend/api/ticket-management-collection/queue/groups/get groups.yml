info:
  name: get groups
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/v1/queue/group"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
