info:
  name: update queue
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/api/v1/queue/:id"
  params:
    - name: id
      value: eb9390d9-ef93-4e13-83f2-dc883def69e3
      type: path
  body:
    type: json
    data: |-
      {
        "description": "this is the queue updated",
        "firstResponseTimeMinutes": 15,
        "resolutionTimeMinutes": 90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
