info:
  name: "add agents "
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/v1/queue/:id/agents"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
