info:
  name: change password
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/api/v1/auth/change-password"
  body:
    type: json
    data: |-
      {
        "currentPassword": "8968585382",
        "password": "123456789",
        "confirmPassword": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
