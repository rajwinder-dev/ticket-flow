info:
  name: details
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/api/v1/auth/details"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
