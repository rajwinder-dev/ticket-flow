info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
          "email": "Reed.Kris22@hotmail.com",
          "password": "123456"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody()
        bru.setEnvVar("accessToken", body.data.accessToken, { persist: true })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
