info:
  name: invite user
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/org/invite"
  body:
    type: json
    data: |-
      {
        email: "xyz@gmail.com",
        roleId: "xyz"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
