info:
  name: delete queue
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/api/v1/queue/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
