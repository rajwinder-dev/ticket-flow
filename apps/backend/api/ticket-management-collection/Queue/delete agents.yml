info:
  name: "delete agents "
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/api/v1/queue/:id/agent"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
