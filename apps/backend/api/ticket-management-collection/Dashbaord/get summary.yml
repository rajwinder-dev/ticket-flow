info:
  name: get summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/dashboard/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
