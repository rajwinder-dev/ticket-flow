info:
  name: get transation history
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/api/v1/ticket/:id"
  params:
    - name: id
      value: "{{ticketId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
