info:
  name: assign Ticket
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{url}}/api/v1/ticket/:id/assign"
  params:
    - name: id
      value: "{{ticketId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "targetType": "AGENT",
        "assignId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
