info:
  name: update priority
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/api/v1/ticket/:id/priority"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "email": "customer1@gmail.com",
        "description": "this is the description",
        "subject": "Login not working "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
