info:
  name: escalate ticket
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/api/v1/ticket/:id/escalate"
  params:
    - name: id
      value: "{{ticketId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
