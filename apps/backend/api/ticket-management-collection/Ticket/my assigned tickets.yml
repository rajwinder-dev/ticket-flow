info:
  name: my assigned tickets
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/ticket/me"
  body:
    type: json
    data: |-
      {
        "email": "customer1@gmail.com",
        "description": "this is the description",
        "subject": "Login not working "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
