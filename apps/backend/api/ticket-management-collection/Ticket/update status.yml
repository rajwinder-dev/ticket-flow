info:
  name: update status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/api/v1/ticket/:id/status"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "email": "customer1@gmail.com",
        "description": "this is the description",
        "subject": "Login not working "
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
