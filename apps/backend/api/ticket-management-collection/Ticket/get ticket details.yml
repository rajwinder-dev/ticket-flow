info:
  name: get ticket details
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/v1/ticket/:id"
  params:
    - name: id
      value: "{{ticketId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
