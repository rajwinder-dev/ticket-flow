info:
  name: get all tickets
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/ticket"
  body:
    type: json
    data: |-
      {
        "email": "customer1@gmail.com",
        "description": "this is the description",
        "subject": "Login not working "
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody()
        bru.setEnvVar("ticketId", body.data[0].id, { persist: true })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
