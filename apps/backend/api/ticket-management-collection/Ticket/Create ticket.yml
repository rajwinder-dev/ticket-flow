info:
  name: "Create ticket "
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/ticket"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
