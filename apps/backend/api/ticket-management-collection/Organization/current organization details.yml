info:
  name: current organization details
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/v1/org/current"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
