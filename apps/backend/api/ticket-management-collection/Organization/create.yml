info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/org"
  body:
    type: json
    data: |-
      {
        "name": "tivenbook2",
        "type": "PERSONAL",
        "slug": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
