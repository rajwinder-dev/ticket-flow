info:
  name: get all members
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/role"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
