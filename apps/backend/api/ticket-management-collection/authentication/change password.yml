info:
  name: change password
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/v1/auth/change-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
