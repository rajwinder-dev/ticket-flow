info:
  name: forget password
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/v1/auth/forget-password/:email"
  params:
    - name: email
      value: test@gmail.com
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
