info:
  name: Signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/auth/signup"
  body:
    type: json
    data: |-
      {
        "username": "test",
        "email": "test2@gmail.com",
        "password": "8968585382",
        "confirmPassword": "8968585382"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
