info:
  name: get providers
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/v1/email"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
