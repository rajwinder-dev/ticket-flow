info:
  name: test mail
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/email/test"
  auth: inherit

runtime:
  variables:
    - name: url
      value: http://localhost

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
