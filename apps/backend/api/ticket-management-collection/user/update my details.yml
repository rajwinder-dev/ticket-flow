info:
  name: update my details
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/api/v1/user/me"
  body:
    type: json
    data: |-
      {
        "location": "testing love"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
