info:
  name: my details
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/user/me"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
