info:
  name: get groups
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/v1/queue-group"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody()
        bru.setEnvVar("queueGroupId", response.data[0].id, { persist: true })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
