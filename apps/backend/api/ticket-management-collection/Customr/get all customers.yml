info:
  name: get all customers
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/customer"
  body:
    type: json
    data: |-
      {
        "email": "customer1@gmail.com",
        "name": "customer1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
