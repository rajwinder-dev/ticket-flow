info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/org"
  body:
    type: json
    data: |-
      {
        "name": "tivenbook2",
        "description": "this is description",
        "teamSize": 23
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
