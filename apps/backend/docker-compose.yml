services:
  backend:
    build: .
    container_name: backend
    ports:
      - "3000:3000"
      - "3001:3001"
    env_file:
      - .env.production
    command: sh -c "npx prisma migrate deploy && npm run serve"
